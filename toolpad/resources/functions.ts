import { useMatch } from 'react-router-dom';
import * as React from 'react';

export function GetPageUrl() {
  // const pageMatch = useMatch('/:slug');
  const [ss, setSS] = React.useState('1123');

  return ss;
}
